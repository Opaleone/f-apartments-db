import { getOneProperty, createProperty, deleteProperty } from "./propertyController";
import { getAllCities, getOneCity, createCity, deleteCity } from "./cityControllers";
import { createFloorplan, deleteFloorplan } from "./floorplanController";
import { createDetails, deleteDetails } from "./detailController";
import { createAverages, deleteAverages } from "./averageController";

export { 
  getOneProperty,
  createProperty,
  deleteProperty,
  getAllCities,
  getOneCity,
  createCity,
  deleteCity,
  createFloorplan,
  deleteFloorplan,
  createDetails,
  deleteDetails,
  createAverages,
  deleteAverages
};